# 2026-03-10 工作日志

## ECCV 2026 补充材料 (suppl.tex)
- 为 EIHMR 论文创建了补充材料模板：`paper/paper-template-Latest/suppl.tex`
- 补充材料包含三个主要 Section：
  - **Section A: Internal Mechanism of MO-SLAM** — 详细描述 MO-SLAM 对 DROID-SLAM 三个阶段（MotionFilter, Frontend, Backend）的修改。核心设计：rendered mesh 在 Frontend 的特征提取层面提供几何结构，但 DBA 中通过 confidence mask 将人体区域权重置零。Frontend 使用 adaptive image replacement（基于 Laplacian texture score），Backend（DBA）始终使用 mask 图片。
  - **Section B: Ablation Visualization** — SA-LHMR（FK Root Correction, Contact-Aware IK）和 MO-SLAM（Timestep Color Encoding, Adaptive Image Replacement）的消融可视化。
  - **Section C: Global Trajectory Visualization** — 多个 EMDB2 序列的全局轨迹展示，与 PromptHMR 和 TRAM 对比。
- 所有图片位置用 `\fbox{TODO}` 占位符标记，待替换为实际图片。
- Section/Figure/Table/Equation 编号使用 A/B/C... 和 S 前缀，与主论文区分。

## MO-SLAM 代码架构理解
- 代码位于 `lib/camera/warmstart_droid.py`，核心类：`WarmstartMotionFilter`, `WarmstartDroidFrontend`, `WarmstartDroid`
- Frontend 和 Backend 的 `factor_graph.py` 中都有 `msk = self.video.masks[iis] > 0; weight[:,msk] = 0.0` 机制
- Adaptive image replacement 通过 `_compute_background_texture_score()` 实现
