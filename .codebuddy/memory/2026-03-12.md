# 2026-03-12 工作记录

## 消融实验可视化脚本
- 创建了 `lib/scripts/concat_ablation_vis.py`：横向拼接 OAR 各阶段 debug 图片（Input + 5 个 debug 子文件夹），支持多进程并行、标签栏、帧采样、缩放等
- 创建了 `scripts/run_concat_ablation_vis.sh`：shell 超参数设置脚本，环境变量可覆盖所有参数
- 输出目录 `oar_ablation_concat` 与源 debug 文件夹并列存放在每个 seq_dir 下
- EMDB 数据集 Input 图片路径: `datasets/EMDB/P{n}/{seq_name}/images/`
- 拼接列顺序: Input → oar_depth_contact_debug → oar_window_pre → oar_window_post_pre_dep → oar_window_post → oar_reproj_error

## 补充材料中文审阅版
- 文件: `paper/paper-template-Latest/suppl_review_cn.md`
- 将原 A 节（MO-SLAM 内部机制，含 DROID-SLAM 背景、三阶段修改详解、信息流总结）精简为"实现细节"
- 新 A 节分为 A.1 SA-LHMR 实现细节 和 A.2 MO-SLAM 实现细节，各约 100-150 字
- 内容聚焦主论文未提及的具体超参数和实现选择，避免与正文 Section 3.2/3.3 重复
- 移除了图 S1（信息流示意图），图编号重新调整为 S1-S4
- 保留 B 节（消融可视化）和 C 节（全局轨迹可视化）不变

## 图 S1 SA-LHMR 消融可视化分析
- 图片: `paper/images/SA-LHMR-Ablation.png` (1806x1869, 4行×5列)
- 列标签: Input | Depth Offset | w/o FK Root Correction | w/o Contact-Aware IK | Ours
- 4行对应4个EMDB2序列：张臂站立、弯腰蹲起、背面行走、正面行走
- 第3-5列使用时间色编码（蓝→绿→橙→红）的多帧SMPL叠加
- Depth Offset列红色标注显示初始相机深度估计误差导致的脚部偏移
- 基于图片更新了 `suppl_review_cn.md` B.1 节，具体描述了FK消除滑步、IK提升场景交互精度

## 图 S2 MO-SLAM 消融可视化分析
- 图片: `paper/images/MO-SLAM-Ablation.png` (1910x527, 4个子图分左右两组)
- 左组(2子图): w/o Timestep Color Encoding vs Ours（室外+室内场景）
  - 内嵌图显示无颜色编码时渲染网格为统一灰色，互相遮挡形成无特征区域
  - 蓝色轨迹严重偏离GT
- 右组(2子图): w/o Adaptive Image Replacement vs Ours
  - 室外场景（纹理匮乏）影响较小，室内场景（纹理丰富）蓝色轨迹明显漂移
  - 说明在背景纹理丰富时覆盖背景反而劣化估计
- 基于图片更新了 `suppl_review_cn.md` B.2 节

## suppl.tex 同步更新
- 删除了 C 节（全局轨迹可视化）及其两个 TODO figure
- A 节从"Internal Mechanism of MO-SLAM"改为"Implementation Details"，分为 A.1 SA-LHMR / A.2 MO-SLAM 两个子节
- B 节用实际图片替换 TODO 占位符：`SA-LHMR-Ablation` 和 `MO-SLAM-Ablation`
- B 节描述文字与 md 审阅版对齐，基于实际图片内容重写
- 最终 suppl.tex 结构：A(实现细节) + B(消融可视化)，无 C 节
